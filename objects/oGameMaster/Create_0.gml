// Create Event of oGameMaster
game_won = false;

// Get all the references once at start
boy = instance_find(oBoy, 0);
girl = instance_find(oGirl, 0);
boy_goal = instance_find(oBoyGoal, 0);
girl_goal = instance_find(oGirlGoal, 0);